doctype 5
html
	head
		title counting with the count

		= stylesheet_link_tag "screen", :media => "screen, projection"
		= javascript_include_tag "application"
		= csrf_meta_tags

	body.container-fluid(class="#{params[:controller]}")

		= yield

		#count